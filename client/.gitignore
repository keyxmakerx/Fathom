# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# The wasm engine module (ADR-0052 §1): "the module ships as a file, never a
# package." scripts/build-wasm.sh writes it here; it is never checked in, the
# same way the retired page's spliced-in module never was either.
public/engine/
